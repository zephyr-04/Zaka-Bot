import { generateAuthorizationSignature, generateAuthorizationSignatures } from "../../../lib/authorization.mjs";
export class PrivyRequestSigner {
    constructor(privyClient) {
        // Expose these as methods here for convenience
        this.generateAuthorizationSignature = generateAuthorizationSignature;
        this.privyClient = privyClient;
    }
    generateAuthorizationSignatures(...input) {
        // Pre-populates the client instance for the function
        return generateAuthorizationSignatures(this.privyClient, ...input);
    }
}
//# sourceMappingURL=request-signer.mjs.map